# `scaffold.bridges`

Every producer/consumer pair whose declared units disagree, and the factor
between them. The config generator reads this table to emit a `[[node]]` doing
the conversion. See [`config_gen`](config_gen.md) for where that happens.

::: satterc.scaffold.bridges
