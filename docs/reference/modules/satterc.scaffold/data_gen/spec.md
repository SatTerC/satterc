# `scaffold.data_gen.spec`

The `Var` table entry, the grid, the generator contexts, and the resolver
that turns a table row into an array. Read this before adding to the
[`daily`](daily.md) or [`static`](static.md) tables.

::: satterc.scaffold.data_gen.spec
